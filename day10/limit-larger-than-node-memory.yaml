apiVersion: v1
kind: Pod

metadata:
  name: limit-demo

spec:

  containers:

  - name: nginx

    image: nginx

    resources:

      requests:

        memory: "128Mi"

      limits:

        memory: "20Gi"
